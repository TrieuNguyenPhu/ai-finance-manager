# ai-finance-manager web (Next.js)

Talks only to `services/gateway-service`.

## Requirements
- Node.js **22+** (prefer **24** LTS)
- pnpm 9+

## Commands
```bash
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm build
```

`NEXT_PUBLIC_API_BASE_URL` defaults to `http://127.0.0.1:8000`.
